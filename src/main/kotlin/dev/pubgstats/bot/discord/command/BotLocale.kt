package dev.pubgstats.bot.discord.command

/**
 * Languages supported by this bot. Adding an entry forces the compiler
 * to flag every [Localized] construction and `when(locale)` that lacks coverage.
 */
enum class BotLocale { EN_US, PT_BR }
